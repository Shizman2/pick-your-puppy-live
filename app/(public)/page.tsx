export default function PublicPreviewPage() {
  return (
    <div style={{ padding: "40px 20px", textAlign: "center" }}>
      <h1 style={{ fontSize: 20, fontWeight: 900, marginBottom: 10 }}>
        Phase 1 Preview
      </h1>
      <p style={{ fontSize: 13, color: "var(--pp-muted)", fontWeight: 600 }}>
        This is a placeholder. It exists only so you can check the header,
        hamburger menu, and bottom nav look and behave correctly. Real
        homepage content gets built in Phase 3.
      </p>
    </div>
  );
}
